const MenuRevealData = (topText, content, bottomText) => {
  let obj = {
    topText: topText || "",
    content: content,
    bottomText: bottomText || "",
  };

  return obj;
};

export default MenuRevealData;
